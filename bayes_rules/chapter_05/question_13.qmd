```{r}
library(bayesrules)
library(tidyverse)
library(fivethirtyeight)
library(ggformula)
```

```{r}
# a
plot_normal(mean = 30, sd = 10)
```

```{r}
# b
data(weather_perth)
gf_histogram(~ temp3pm, data = weather_perth)
```

```{r}
# c
weather_summary <- weather_perth |>
  summarize(mean_temp = mean(temp3pm), sample_size = n())
summarize_normal_normal(mean = 30, sd = 10, sigma = 5, y_bar = weather_summary$mean_temp, n = weather_summary$sample_size)
```

```{r}
# d
plot_normal_normal(mean = 30, sd = 10, sigma = 5, y_bar = weather_summary$mean_temp, n = weather_summary$sample_size)
```


