```{r}
library(bayesrules)
library(tidyverse)
library(fivethirtyeight)
library(mosaic)
```

```{r}
data("wwc_2019_matches")
wwc_2019_matches <- wwc_2019_matches %>%
  mutate(total_goals = score1 + score2)
```

```{r}
# a
plot_gamma(shape = 1, rate = 0.25)
summarize_gamma(shape = 1, rate = 0.25)
```

```{r}
# c
gf_histogram(~ total_goals, data = wwc_2019_matches)
```

```{r}
# d
summarize_gamma_poisson(shape = 1, rate = 0.25, sum_y = mean(wwc_2019_matches$total_goals), n = nrow(wwc_2019_matches))
```

```{r}
# e
plot_gamma_poisson(shape = 1, rate = 0.25, sum_y = mean(wwc_2019_matches$total_goals), n = nrow(wwc_2019_matches))
```


